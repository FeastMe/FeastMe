{# Citation for creating tables in Jinja:
Date: 12/04/2022 
Sourced from GitHub Reposotiry osu-cs-340-ecampus/flask-starter-app 
URL: https://github.com/osu-cs340-ecampus/flask-starter-app/blob/master/bsg_people_app/templates/people.j2 #}

{% extends 'base.j2' %}
{% block content %}

<html>
<body>
    <div>
        <h1>Editing Driver</h1>
        <table>
            {% if driver %}
                <thead>
                    <tr>
                        {% for key in driver[0].keys() %}
                            <th>{{ key }}</th>
                        {% endfor %} 
                    </tr>
                <thead>
                <tbody>
                    {% for item in driver %}
                    <tr>
                        {% for key in item.keys() %}
                            <td>{{ item[key] }}</td> 
                        {% endfor %} 
                    </tr> 
                    {% endfor %} 
            {% endif %} 
        </table>

    {# Citation for creating a form for CRUD operations in Jinja:
    Date: 11/10/2022
    Sourced from GitHub Reposotiry osu-cs-340-ecampus/flask-starter-app
    URL: https://github.com/osu-cs340-ecampus/flask-starter-app/blob/master/bsg_people_app/templates/edit_people.j2 #}

        <div id="edit">
            <form id="editDriver" action="/edit_driver/{{driver[0].account_id}}" method="post">
                <h3>Edit Driver</h3>
                <fieldset>
                    <input type="hidden" value="{{driver[0].account_id}}" id="driver_id" name="driver_id" required>
                    
                    <label for="license_placte">License plate:</label>
                    <input type="text" value="{{driver[0].license_plate}}" id="license_plate" name="license_plate" required>
                    
                    <label for="license_number">License number:</label>
                    <input type="text" value="{{driver[0].license_number}}" id="license_number" name="license_number" required>
                    
                    <br><br>

                    <input type="submit" value="Save" name="Edit_Driver" class="button">
                    <a href='/drivers'><input class="button" type="button" value="Cancel"></a>
                </fieldset>                   
            </form>

</html>
{% endblock %}